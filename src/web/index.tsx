import init from '../Loader'
/**
 * Web View Index file
 * TODO: Argument detection from HTML config
 */
const el = document.createElement('div')
el.id = 'oxrti'
document.body.appendChild(el)
init(el)
